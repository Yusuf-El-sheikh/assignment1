// assignment #1
// part #2

/* Q - 1

*for each* is a method that executes a specific provided function for every element in the array 
-you can also use arrow format-. 

*for of* just makes a loop that iterates over an object that is iteratable, 
it doesnt require you to use any indexing to access the elements of said object, it automatically collects it.

*/

/* Q - 2
    
hoisting : hoisting is the behaviour that js does right before executing, which is moving variable declarations and 
functions declarations and allocating them at the very top of the program.

temporal dead zone : is the amount of time of which the declaration of a variable or a function is withing its scope block,
yet is still inaccessable because its before the original declaration. 

-this is why you cant use a variable declared with let before its defintion btw.- 
*/

/* Q - 3

" == " only compares the value of two variables, it also coerces different data types for example 1 == '1' is true,
" === " compares both value and types so 1 == '1'. 
*/

/* Q - 4


*/

/* Q - 5 

coercion is when js changes a type to another, without user input.

type casting or conversion is when YOU change it on command using constructor methods or other ways.
*/